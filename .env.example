# JWT Settings
JWT_SECRET=change_me_in_production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=30

# Redis Settings
REDIS_URL=redis://redis:6379/0

# RabbitMQ Settings
RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672/

# Database Settings
DATABASE_URL=postgresql+psycopg2://app:app@postgres:5432/ossdb

# MinIO Settings
MINIO_ENDPOINT=http://minio:9000
MINIO_ACCESS_KEY=minio
MINIO_SECRET_KEY=minio123
PRESIGN_TTL_SECONDS=600

# Default User Credentials
DEFAULT_ADMIN_EMAIL=admin@demo.gov
DEFAULT_ADMIN_PASSWORD=Admin!234
DEFAULT_USER_EMAIL=user@demo.gov
DEFAULT_USER_PASSWORD=User!234

# Service URLs (for inter-service communication if needed)
AUTH_SERVICE_URL=http://auth-service:8001
STORAGE_SERVICE_URL=http://storage-service:8002
AUDIT_SERVICE_URL=http://audit-service:8003
